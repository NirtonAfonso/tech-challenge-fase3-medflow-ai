# ---------------------------------------------------------------------------
# Dependências de EXECUÇÃO do assistente (sem GPU, sem treinamento).
# O fine-tuning tem arquivo próprio: requirements-training.txt
# ---------------------------------------------------------------------------

# Núcleo
python-dotenv>=1.0,<2.0
numpy>=1.26,<3.0

# LangChain / LangGraph (linha 1.x)
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-community>=0.4,<1.0
langchain-text-splitters>=1.0,<2.0
langgraph>=1.0,<2.0

# Leitura opcional de PDFs externos (PCDTs oficiais)
pypdf>=5.0,<7.0

# Testes
pytest>=8.0,<10.0
pytest-cov>=6.0,<8.0
